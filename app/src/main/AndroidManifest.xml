<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vladimirbabenko.android_base_homeworks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:screenOrientation="landscape" -->
        <!-- android:screenOrientation="portrait" -->
        <activity
            android:name=".MainActivity"
            android:label="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivityHomeWorkScreen"
            android:label="Sign In" />

        <activity
            android:name=".OtherTaskActivity"
            android:label="Other Tasks" />

        <activity android:name=".CustomUserInfoActivity" />

        <activity android:name=".card.DynamicGuiActivityOld" />
        <activity android:name=".DynamicGuiActivity" />
        <activity android:name=".card.MainActivityTest" />


    </application>

</manifest>